import styled from 'styled-components/native';

export const CartSize = styled.Text`
  color: #fff;
  position: absolute;
  background: #7159c1;
  padding: 0 5px;
  border-radius: 10px;
  top: -10;
  right: 0;
`;
